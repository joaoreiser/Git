#BN_16_0_0_0_0

Simple CNN with batch normalization ready for training, fully integrated
with TF_Reader_2 file.
Some constants and paths must be changed to fit your application.

##################################################################################

#BN_32_32_32_0_0
#CNN_32_32_0_32_0

BN and CNN files show an example of convolutional neural network with TensorFlow.
BN contains batch normalization. CNN does not. 
Both generate the save files of the networks. 

##################################################################################

#Freeze 

File that converts the save files of TensorFlow in a freeze weights file (.pb). 
This .pb file can be used for production or in a step to create .graph file for
Movidius.

##################################################################################

#TF_Reader_2

File that read TFRecord files of TensorFlow
The code present on it is ideal for training neural networks.
Normalization, Shuffle, Repeat and Batch Size Configurations are available and
can be changed, as like the image shape. 
Ideally the image size must be set when generating the TFRecord file.

##################################################################################

#TFRecord

Python code to generate TFRecord file with TensorFlow.
The code sample is used for a two classes dataset (Heal and Sick). The
folders path with the pictures and generated file path+name must be informed. 
In this case, both the pictures folders must have the same amount of samples.

##################################################################################

Feel free to clone and adept all codes for your need.
